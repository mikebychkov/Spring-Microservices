spring:
  application:
    name: licensingservice
  profiles:
    active: default
  #config:
  #  import: "http://localhost:8888"
  cloud:
    config:
      enabled: true
  data:
    rest:
      base-path: "/api"